# 08 — Services and Business Rules

This document describes what each `@Service` class does and lists the
non-obvious invariants future agents must preserve. Validation
rules are also cross-referenced in [03-validation-and-errors.md](./03-validation-and-errors.md).

## Conventions

- Services are `@Service` beans annotated `@Transactional`
  (read-only on read paths, default on writes).
- Services delegate persistence to Spring Data repositories.
- Services own the entity ↔ DTO mapping via static `Mapper` classes;
  detail responses are produced *inside* the transaction so that
  `@EntityGraph` detail queries can resolve nested data.
- Many-to-many association mutations replace the full set when the
  corresponding id list is defined in a PATCH request.

## SiteService (`site/`)

Responsibilities:

- `search(q, country, page, size, sort)` — paginated public list.
  `q` is trimmed; blank `q` is null. `country` is an exact match.
- `getDistinctCountries()` — returns the deduplicated list of
  countries (used by `/api/sites/countries`).
- CRUD: `getById`, `create`, `update`, `delete`.
- Uniqueness on `siteName` is enforced by the database; conflicts
  return HTTP `409` with a refined message.

## CultureService (`culture/`)

- Search, CRUD, `cultureFeature` management through the
  `Culture.features` collection.
- Enforces `startBp >= endBp` at service level (matches DB CHECK).

## ArchaeologicalContextService (`archaeological_context/`)

- Filters: `q`, `siteId`, `cultureId`.
- Detail fetch uses
  `ArchaeologicalContextRepository.findDetailById`, which eagerly
  loads `site`, `culture`, `culture.features`, and `references`.
- `update(...)` replaces `referenceIds` (many-to-many to
  `bibliographic_reference`) when the field is defined in the PATCH
  request.

## IndividualService (`individual/`)

Responsibilities:

- `search(...)` returns full `IndividualResponse` records via a
  two-step query: paginated id query then
  `findDetailsByIndividualIdIn(...)` with `@EntityGraph` to populate
  context/site/culture/bones/skeletons/funerary contexts in one
  follow-up.
- `searchSummary(...)` powers `GET /api/individuals/list` — it
  returns `IndividualSummaryDto` directly, projected from the
  database (no entity hydration).
- CRUD: `getById`, `create`, `update`, `delete`.

Allowed sort fields:

```
individualId, individualName, individualType, ageAtDeathMin,
ageAtDeathMax, ageClassMain, sex, updatedAt
```

Default sort: `individualId,asc`.

### Validation (`validateIndividual`)

- non-null `individualType`, `ageClassMain`, `sex`, `sexCertain`;
- `ageAtDeathMin <= ageAtDeathMax` when both present;
- `mni` matches `^\?$|^[1-9][0-9]*$`;
- `INDIVIDUAL` ↔ `mni = '1'`;
- `MIXED_INDIVIDUALS` ↔ `mni != '1'`;
- `UNASSIGNED_REMAINS` allows any valid mni.

`mni_statistical` is generated by the database; the service must
never write to it. The JPA entity declares it
`insertable = false, updatable = false`.

## BoneService and AdminBoneService (`bone/`)

- Public `BoneService`: search with `q`, `boneCatalogId`,
  `individualId` (the `individualId` filter matches through the
  `bone_individual` bridge via an `EXISTS` subquery); CRUD.
- `AdminBoneService` powers `/api/admin/bones` with an admin list
  projection (`AdminBoneListResponse`).
- `BoneMapper` rejects blank `boneSource` and validates
  `boneQuantityMin >= 1`.
- `boneCatalogId` reference is required; `ON DELETE RESTRICT` on the
  FK means a catalog entry cannot be deleted while bones reference
  it.

Curatorial fields (`specimenName`, `repository`, `yearDiscovery`,
`boneProcessing`) live on `Bone` directly — they are intentionally
not normalized into a separate `specimen` table.

- A bone is associated with **one or more** individuals through the
  `bone_individual` bridge (`Bone` owns the `@ManyToMany`). `create`
  resolves all `individualIds`; `update` replaces the whole association
  set when `individualIds` is defined. Both call
  `RemainsExclusivityGuard.assertCanAddBones(individualIds)` *inside the
  transaction*, which locks every target individual and rejects (HTTP
  `409`) if any of them already holds a skeleton (see the guard below).
  At least one individual is required; duplicates are ignored.

## SkeletonService (`skeleton/`)

- Paginated search with `q`, `individualId`, `skeletonCategory`;
  CRUD.
- Validates `preservationIndex ∈ [0, 100]` when present.
- Curatorial fields live on `Skeleton` directly (same rationale as
  `Bone`).
- `create` calls `RemainsExclusivityGuard.assertCanAddSkeleton`, and
  `update` (when `individualId` changes) calls
  `assertCanReassignSkeleton`; creating/reassigning a skeleton for an
  individual that already has bones — or a second skeleton — is rejected
  with HTTP `409` (see the guard below).

`Skeleton` is a whole-skeleton preservation record. It is not a
collection of bones. Do not collapse it into `Bone`.

## RemainsExclusivityGuard (`individual/`)

Enforces the scientific-integrity invariant on every server-side write
that attaches remains to an `Individual`:

> An individual may have **one or more `Bone` records** **or** **one
> general `Skeleton` record**, but **never both**, and **at most one
> skeleton**.

- Each method (`assertCanAddBone` / `assertCanAddBones`,
  `assertCanAddSkeleton`, `assertCanReassignSkeleton`) first takes a
  `PESSIMISTIC_WRITE` row lock on the parent individual via
  `IndividualRepository.findByIdForUpdate` (`SELECT … FOR UPDATE`), then
  checks the *other* table. The bone path checks for an existing skeleton
  per individual; the skeleton path checks for associated bones with
  `BoneRepository.existsByIndividuals_IndividualId` (through the
  `bone_individual` bridge). `assertCanAddBones` simply locks and checks
  each id in turn, so attaching a bone to several individuals validates
  them all before anything is written. Locking before checking serialises
  concurrent writes for the same individual, so two transactions cannot
  each pass the check and both write the opposite remains type. The methods
  must run inside the caller's `@Transactional` method and return the
  already-locked `Individual`(s) so the service avoids a redundant load.
- Violations throw `RemainsExclusivityException`, mapped by
  `GlobalExceptionHandler` to HTTP `409` with the stable code
  `INDIVIDUAL_REMAINS_EXCLUSIVITY` and `fieldErrors.individualId` (see
  [03-validation-and-errors.md](./03-validation-and-errors.md)).
- **Defense in depth:** the database also carries a unique index
  (`uq_skeleton_one_per_individual`) and two cross-table `BEFORE` triggers
  (`trg_bone_remains_exclusivity`, `trg_skeleton_remains_exclusivity`).
  These close the race window and protect direct DB writes;
  `GlobalExceptionHandler` maps the resulting SQLSTATE `23505`/`23514`
  back to the same Problem Detail. Schema management is manual (no migrations
  runner exists): for an existing populated database, run the audit queries
  inlined in `database.sql` next to the exclusivity index/triggers first —
  conflicting rows are surfaced, never auto-fixed.

### Moving an individual between archaeological contexts

`IndividualService.moveContext(id, destinationContextId)` (exposed as
`POST /individuals/:id/move-context`) reassigns an individual's mandatory
`ArchaeologicalContext`. An individual must always belong to **exactly one**
context, so there is no unlink/orphan form: the destination is required, must
exist (`404` otherwise) and must differ from the current context (`400`).

> **Scientific-consistency rule.** A `FuneraryContext` is a burial grouping
> **inside** a single archaeological context, so its member individuals must
> share that context. The move is therefore **blocked** — nothing is mutated —
> when the individual still belongs to any funerary context whose archaeological
> context differs from the destination. This is a *block*, not a silent detach:
> the editor must first remove the individual from those funerary contexts.

- The guard (`assertNoBlockingFuneraryContexts`) reads the individual's funerary
  memberships with their owning context via
  `FuneraryContextRepository.findMembershipsByIndividualId` and throws
  `IndividualContextReassignmentBlockedException`, mapped by
  `GlobalExceptionHandler` to HTTP `409` with the stable code
  `INDIVIDUAL_CONTEXT_REASSIGNMENT_BLOCKED` and a `blockingFuneraryContexts`
  array (see [03-validation-and-errors.md](./03-validation-and-errors.md)).
- Validation and the write to the individual's own context run in the same
  `@Transactional` method, so the check-then-set is atomic for that row. No
  extra row locking is taken: the individual lock would not prevent the only real
  race (a concurrent funerary-membership add via the `FuneraryContext` side), and
  concurrent moves are last-write-wins without inconsistency.
- **Related records are left untouched.** Bones (context-independent, shared via
  `bone_individual`) and the individual's skeleton travel with the individual;
  their bone-/skeleton-level dating records are unaffected, and context-level
  dating records stay attached to the source context.
- The **same guard also runs on `IndividualService.update`** whenever a `PATCH`
  changes `archaeologicalContextId`, so the inconsistent state cannot be created
  through the general edit form either.

## BoneCatalogService (`bone_catalog/`)

- Search, hierarchical browsing through `parentBoneCatalogId`, and
  CRUD on the controlled vocabulary.
- The view `bone_catalog_component` is read-only and exposed only as
  a Hibernate subselect via `BoneCatalogComponent` / `BoneCatalogComponentRepository`.
- Seed data must use **stable explicit `bone_catalog_id` values**,
  because the view's grouped→component mappings are hard-coded.
  Reassigning catalog ids breaks the view.
- Do not use `component_quantity` as observed quantity (that's
  `bone.bone_quantity_min`).

## DatedSampleService (`dated_sample/`)

- Search filters: `sampleOrigin`, `datingType`,
  `archaeologicalContextId`, `boneId`, `skeletonId`.
- Detail fetch uses
  `DatedSampleRepository.findDetailById`, which eagerly loads
  `archaeologicalContext` (with site + culture), `bone` (with
  catalog and individual), `skeleton` (with individual),
  `datingResults`, and `datingResults.datingTechnique`.
- Service-level invariants (`validate`):
  - exactly one of `archaeologicalContext`, `bone`, `skeleton` is
    non-null;
  - `sampleOrigin` matches that non-null reference;
  - `CONTEXT` cannot have `datingType = DIRECT`;
  - `BONE` and `SKELETON` cannot have `datingType = INDIRECT`.

On PATCH, the service merges the current origin ids with the
defined ones from the update DTO and revalidates the result, so a
client can switch the origin in a single call.

## DatingResultService (`dating_result/`)

- Filters: `datedSampleId`, `datingTechniqueId`.
- Requires both `datedSampleId` and `datingTechniqueId` on create.
- Technique deletion is restricted by `ON DELETE RESTRICT` in the
  schema; in-use techniques cannot be removed.

## DatingRecordService (`dating_record/`)

- Backoffice/admin aggregate over **one `DatedSample` + its
  `DatingResult[]`** (not a new table). Serves
  `/api/admin/dating-records`. The public `/api/dated-samples` and
  `/api/dating-results` services are untouched.
- Reuses `DatedSampleRepository`/`DatingResultRepository` (no schema
  change). Detail responses use the lighter
  `DatedSampleRepository.findRecordDetailById` (eager-loads only
  `datingResults` + `datingTechnique`; origin ids come from the lazy
  `ManyToOne` proxy ids, so the heavier site/culture/bone graph is not
  loaded).
- Re-applies the same sample invariants as `DatedSampleService.validate`
  (exactly one origin; origin matches `sampleOrigin`; `CONTEXT`→
  `INDIRECT`/`UNCERTAIN`; `BONE`/`SKELETON`→`DIRECT`/`UNCERTAIN`) but
  raises `DatingRecordValidationException` (HTTP `422`, stable codes) and
  additionally requires **≥1 dating result**.
- **Atomicity:** `create`/`update`/`delete` are each `@Transactional`.
  Child techniques are resolved and the rules validated **before** any
  write/replace, so an invalid request persists nothing; the transaction
  boundary is the backstop for flush-time failures.
- **PATCH result strategy:** replace-the-whole-set —
  `datingResults` absent leaves children unchanged; present replaces the
  full set transactionally (recreated via `orphanRemoval`). Delete relies
  on `orphanRemoval` + DB `dating_result … ON DELETE CASCADE`.

## DatingTechniqueService (`dating_technique/`)

- Vocabulary table. `datingTechniqueName` is required and unique.
- Embed response (`DatingTechniqueEmbedResponse`) is used inside
  `DatingResultResponse` to avoid recursive nesting.

## FuneraryContextService (`funerary_context/`)

- Filters: `archaeologicalContextId`, `burialContext`.
- CRUD plus member-set replacement.
- Validates `burialContext`/`burialType` consistency:
  - `NO` ⇒ `burialType` must be null;
  - `YES` ⇒ `burialType` must be non-null.
- PATCH with a defined `individualIds` list **replaces** the
  membership through `funerary_context_individual`.

## ReferenceService (`reference/`)

- Search by `q` across bibliographic fields (authors, year, title,
  journal, doi). The exact JPQL is defined in
  `ReferenceRepository`.
- CRUD; bridge collections to `Site` and `ArchaeologicalContext` are
  managed from the parent side (PATCH on `Site`/`ArchaeologicalContext`),
  not from `Reference` itself.
- References cover three types (journal article, article/chapter in a
  book, complete book). Only `authors` and `year` are required; `title`
  and `journal` are optional (the work title is in `title` for articles
  and in `book` for complete books). `ReferenceMapper` rejects the
  invalid combination `isBook == true && isArticleInBook == true`
  (mirrors the `chk_bibliographic_reference_type_flags` DB constraint)
  with an `IllegalArgumentException` → `400`, checked against the merged
  entity so partial PATCH updates are covered. `book` is intentionally
  not required when `isBook` is true, so incomplete imported references
  can still be saved.

## StatsService (`stats/`)

- `getHomeStats()` — single-pass aggregates:
  `IndividualRepository.sumMniStatistical()`,
  `BoneRepository.sumBoneQuantityMin()`, plus row counts for `site`
  and `skeleton`. Returns `HomeStatsResponse` with the canonical
  `lastUpdatedAt` (max `updated_at` across the public domain
  tables; null when every relevant table is empty).
- `getMapTimelineStats()` — composes three projection queries:
  - `SiteRepository.findMapTimelineSiteRows()`
  - `ArchaeologicalContextRepository.findMapTimelineUnitRows()`
  - `CultureRepository.findMapTimelineCultureRows()`
  - then folds per-site aggregates (`totalMni`, `individualCount`,
    `boneCount`, `skeletonCount`, `datedSampleCount`,
    `dominantCultureId`, `cultureIds`) over the unit rows.
  - `boneCount` per context is `COUNT(DISTINCT bone.bone_id)` joined
    through `bone_individual`, so a bone shared by several individuals in
    the context counts once. `getHomeStats` /
    `AdminDashboardStatsService` count or sum `bone` rows directly (no
    bridge join), so they are inherently free of this double-count risk.

## AdminDashboardStatsService (`stats/`)

- Returns `AdminDashboardStatsResponse` with row counts for `site`,
  `archaeological_context`, `individual`, `bone`, `bone_catalog`,
  `skeleton`, `bibliographic_reference`, `culture`,
  `funerary_context`, `dating_technique`, `dated_sample`,
  `dating_result`, `app_user`.
- Counts are simple `JpaRepository.count()` calls.

## BoneSiteSearchService (`bone_site_search/`)

- Builds a `BoneSiteSearchCriteria` from query params and delegates
  to the custom `BoneSiteSearchRepository` (impl in
  `BoneSiteSearchRepositoryImpl`).
- The repository issues a **single JPQL constructor projection**
  joining `Bone → Bone.individuals (Individual) → ArchaeologicalContext →
  Site (LEFT JOIN Culture)`, plus `BoneCatalog`, with
  `SIZE(b.datedSamples)` per match. The bridge join produces one row per
  (bone, individual) pair.
- Aggregation, sorting, and pagination run in Java on the flat row
  list inside the service. Bone-keyed sets deduplicate by `bone_id`, so a
  bone associated with several individuals counts once in the bone /
  remain totals while every individual is counted in `individualCount`.
- Repository-level `setMaxResults(app.search.bone-site.max-match-rows)`
  caps the broad-match budget.
- The controller caps page size at
  `app.search.bone-site.max-page-size`.

## DatasetExportService (`dataset/`)

- Builds the ZIP archive returned by `GET /api/dataset/download`.
- Writes UTF-8 CSV entries via `CsvWriter`, one per scientific table plus one per
  bridge relation. `bones.csv` no longer has an `individual_id` column; the
  bone↔individual links are exported as a separate `bone_individuals.csv` bridge
  file (`bone_id`, `individual_id`), alongside `funerary_context_individuals.csv`,
  `site_references.csv`, and `archaeological_context_references.csv`.
- `references.csv` exports the full `bibliographic_reference` row, including
  `editor`, `book`, `corporate_author`, `city`, `is_book`, and
  `is_article_in_book` (no nullable fields are silently dropped).
- Every CSV is written with a header row and a deterministic row order:
  main tables sorted by primary key via `findAll(Sort)`, bridge/relation rows
  sorted by their key, so repeated exports of the same data are byte-stable.
- Bundles three generated entries with the CSVs: `README.md` (researcher-facing
  scope, citation, and license; see `DatasetReadme`), `manifest.csv`
  (`file_name`, `source`, `row_count`, `notes` — actual per-file row counts make
  a missing table obvious), and `metadata.csv` (key-value dataset metadata such
  as `generated_at_utc`, `license`, `source_article_doi`, `schema_model_version`).
- Caches the archive bytes for
  `app.dataset.export.cache-ttl-seconds`.
- Enforces `app.dataset.export.max-bytes` as a hard upper bound.
- Excludes authentication/session tables (`app_user`, `refresh_token`), internal
  users, deprecated v1 tables, and the `bone_catalog_component` view.
- Logs `dataset_download` audit events through
  `SecurityAuditService`.

## UserService (`user/`)

- Admin-only CRUD on `AppUser`; self-service `getByUsername` and
  `changePassword`.
- `changePassword` revokes refresh tokens on success (the new
  password should invalidate prior sessions). *Requires code
  verification — `UserService.changePassword` calls into
  `RefreshTokenService`*; check the current implementation if the
  exact revocation list matters.
- Password hashing via `PasswordConfig.passwordEncoder()` (BCrypt).
- Username collisions raise `DataIntegrityViolationException`,
  mapped to HTTP `409` with a refined message.

## RefreshTokenService (`refresh_token/`)

- `storeNew` — persists hashed refresh tokens with family,
  per-token expiry, and family expiry.
- `consumeForRotationOrThrow` — atomic conditional update to consume
  the current token. Zero rows updated implies replay / invalid /
  already-rotated; the controller revokes the family.
- `linkReplacement` — writes `replaced_by_token_id` on the old row.
- `revokeFamily` — revokes all active tokens in a family.
- `RefreshTokenCleanupJob` periodically deletes expired rows.

`TokenHashService` produces the deterministic hash stored in
`refresh_token.token_hash`; the raw token never persists. Detailed
flow lives in [02-security-and-auth.md](./02-security-and-auth.md).

## Cross-cutting helpers

- `common/util/MapperUtils` — `JsonNullable` checks, string
  normalization, required-non-blank assertions.
- `common/util/PaginationUtils` — `Pageable` parsing with an
  allow-list of sort fields and page-size cap (`MAX_PAGE_SIZE =
  100`).
- `common/util/EntityFetchUtils` — small helpers for repository
  fetch-or-throw patterns.
- `common/summary/SummaryMappers` — single source for the flat
  summary DTOs used inside response records.

## Non-obvious behaviors to preserve

- `mni_statistical` is **DB-generated**; never write it from Java
  and never accept it in create/update requests.
- `updated_at` is **trigger-maintained**; never set it from Java.
- `bone_catalog_component` is a view with hard-coded ids; do not
  regenerate catalog ids without updating the view.
- Many-to-many PATCH operations replace the full set. If you need
  add-one / remove-one semantics, add new endpoints instead of
  redefining the existing PATCH behavior.
- The Individual remains-exclusivity invariant (bones XOR skeleton, at
  most one skeleton) is enforced by `RemainsExclusivityGuard` with a
  pessimistic lock inside the write transaction, **plus** a DB unique
  index and cross-table triggers. Any new write path that attaches a
  bone or skeleton to an individual must go through the guard. Do not
  replace the lock with a preflight read outside the transaction.
- `IndividualService.search` runs the count query and the id query
  with a paged sort, then re-loads the rows via an entity-graph
  detail query. This avoids N+1 fan-out across nested associations
  but means total query count is `count + ids + detail`. Preserve
  this two-step shape unless you replace it with a single
  constructor projection.
- `BoneSiteSearchService` returns aggregated rows produced in-memory
  from a single JPQL projection — do not refactor it into per-row
  follow-up queries.
- `StatsService.getMapTimelineStats` runs three projection queries.
  The frontend `MapTimelineStatsService` uses `shareReplay`, so a
  single client typically pays for the round-trip once. Backend
  changes that increase the response size or change the row shape
  will affect public web performance.
